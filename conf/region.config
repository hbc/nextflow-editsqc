// The region now travels with each sample via `meta.region` (see the samplesheet),
// so SAMTOOLS_VIEW_REGION is configured once in conf/module.config and this file
// no longer needs to be loaded after the profiles to pick up params.region.

