process {
    withName: SAMTOOLS_VIEW_REGION {
        publishDir = [path: "${params.outdir}/minimap2/genome/region", mode: 'copy']
        ext.args = '-h -b -F 0x4'
        ext.args2 = "${params.region}"
    }
}
